# go-everyday

## Go Tabel Driven Test
Go developers prefer Table-Driven Tests. instead of writing separate functions for every scenario (TestAddPositive, TestAddNegative, TestAddZero), you define a list of test cases (a "table") and loop over them.